<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Some samsung boards have different led device control mechanism -->
    <bool name="useSamsungLedDevice">true</bool>
    <!-- Full path to the sysfs toggle -->
    <string name="flashDevice">/dev/ledflash</string>

    <!-- Off setting -->
    <integer name="valueOff">0</integer>
    <!-- On setting -->
    <integer name="valueOn">1</integer>

    <!-- Low setting -->
    <integer name="valueLow">-1</integer>
    <!-- Middle setting -->
    <integer name="valueHigh">-1</integer>
    <!-- Highest setting, leave -1 if it doesn't exist -->
    <integer name="valueDeathRay">-1</integer>
</resources>
